/// <reference types="node" />
import { ConsoleAppender, LoggerConfiguration } from "../core/LoggerConfig";
import { LogLevel } from "../core/LoggerLevel";
export declare class NodeEmojiAppender extends ConsoleAppender {
    constructor(console: Console);
    formatLevel(level: LogLevel): string;
    formatDate(now: Date): string;
    formatName(name: string): string;
    formatArg(arg: any | null): string;
    formatDump(obj: any): string[];
}
export declare const colorEmojiConfig: LoggerConfiguration;
